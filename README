# Phabricator Development Environment

Make sure Vagrant and Ansible are installed. Setting up:

 * `git clone https://github.com/putrasattvika/oss-midterm.git`
 * `cd oss-midterm`
 * `vagrant box add ubuntu/xenial64`
 * `vagrant up`
